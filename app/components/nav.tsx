import ClientNav from "./client-nav";

// Active nav background color - #F9E0E2

// This is a server component that wraps the client component
export const Nav = () => {
  return <ClientNav />;
};

export default Nav;
