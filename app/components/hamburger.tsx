import ClientHamburger from "./client-hamburger";

// This is a server component that wraps the client component
export const Hamburger = () => {
  return <ClientHamburger />;
};

export default Hamburger;
