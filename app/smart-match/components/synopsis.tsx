// import { Textarea } from "@/app/ui-primitives/textarea";
import React from "react";
// import { FormState } from "../page";

const Synopsis = () => {
  return (
    <div className="w-full">
      <label className="font-semibold mb-2 block">Plot Beats or Synopsis</label>
      {/* <Textarea
        value={form.synopsis}
        onChange={(e) =>
          setForm((prev) => ({ ...prev, synopsis: e.target.value }))
        }
        rows={5}
        className="w-full h-22"
      /> */}
    </div>
  );
};

export default Synopsis;
